## Installation and running

Using npm:

```$ npm install```

```$ npm dev```

Using yarn:

```$ yarn```

```$ yarn dev```

## Build and deploy

```npm build && firebase deploy```

```yarn build && firebase deploy```